# P0 — Project scaffolding

## Built

- `pyproject.toml` — hatchling backend, Python ≥3.11, runtime deps
  `numpy`/`scipy`/`pandas`, dev extras `pytest`/`ruff`/`pyright`/`statsmodels`/`formulaic`.
- `ruff.toml` — py311 target, line length 100, selects E/W/F/I/UP/B/SIM/NPY,
  excludes `notebooks/`, `.venv`, `build`, `dist`.
- `pyrightconfig.json` — basic typecheck on `model_crafter/` and `tests/`,
  excludes `notebooks/`.
- `.github/workflows/ci.yml` — runs `ruff check`, `pyright`, `pytest` on push and PR.
- `model_crafter/__init__.py` — empty package stub with `__version__ = "0.0.0"`.
- `tests/__init__.py`, `notes/.gitkeep` — empty.
- `README.md` — one paragraph plus links to DESIGN.md and AGENTS.md.
- Deleted legacy `crafter/`, `requirements.txt`, `Makefile`, `ReadMe.md`.
- Extended `.gitignore` for `.venv/`, `.vscode/`, `.pytest_cache/`, `.ruff_cache/`.

## Verified

- `uv pip install -e .[dev]` resolves and installs in the existing `.venv`
  (Python 3.11.15).
- `pytest` → collected 0 items, exits 0.
- `ruff check .` → All checks passed.
- `pyright` → 0 errors, 0 warnings, 0 informations.

## Open

- `notebooks/linreg_prostate_cancer.ipynb` still imports the deleted
  `crafter.*` modules; excluded from lint/typecheck. Decide later whether
  to delete it outright or port to `model_crafter` once Phase 1 lands.
- Stability-check defaults and other items in DESIGN.md §11 ("Open
  questions") are unresolved — should be settled before Phase 4.
- CI matrix is single-Python (3.11). Expand to 3.12/3.13 once
  dependencies and code prove stable.
